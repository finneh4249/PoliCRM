# Firebase Configuration
# Path to your Firebase Service Account JSON file (for backend)
FIREBASE_CREDENTIALS_PATH=firebase-service-account.json

# Encryption Key for PII (Generate using cryptography.fernet.Fernet.generate_key())
ENCRYPTION_KEY=

# Session Secret (for cookie signing if needed)
SECRET_KEY=change_this_to_a_random_secret_string
